import { describe, it } from "vitest";

describe("PollingService", () => {
  describe("startPolling", () => {
    it.todo("should poll GitHub repository at configured intervals");
    it.todo("should filter out PRs with claim label");
    it.todo("should use Effect.repeat with Schedule.spaced");
    it.todo("should log each poll cycle");
  });
});
